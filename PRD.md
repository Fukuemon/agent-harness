# AI エージェントにプロジェクト固有の知識とガードレールを与える agent-harness PRD

**Status:** Draft

**Owner:** Fukuemon

**Reviewers:** 未定

## Overview / Problem Statement

agent-harness は、AI エージェントにプロジェクト固有の知識とガードレールを与える、小さなパッケージと運用ルールである。  
開発プロセスの標準も定める。具体のワークフローのハーネスは、利用者が必要に応じて選んで導入する。

Claude Code、Codex CLI、Cursor へ導入する規約・スキル・プラグイン・フック・設定を、この文書ではまとめてハーネスと呼ぶ。  
ハーネスの内容は、モデルが進化しても必要なものと、進化に伴って不要になるものに分かれる。

- テストコマンドや業務知識のように、モデルだけでは分からない事実は、モデルが進化しても要る。
- 段階を踏んで考えさせる指示や役割分割のように、モデルの弱さを補う指示は、モデルの更新で不要になり、挙動を縛る負債になる。

もう 1 つの分け方がある。どのプロジェクトでも同じでよい内容と、プロジェクトごとに違う内容である。  
前者は 1 か所で管理して各プロジェクトから使い、各プロジェクトには後者だけを置きたい。

これらを区別せずに積み上げた結果、次の問題が起きている。

- モデルが更新されても、どの指示を外してよいか判断できない。
- 設計文書に経緯と変更履歴が入り込み、現在の事実を読み取りにくい。実装とずれても検出できない。
- エージェントに書かせた spec は長く整って見えるが、読み手が検証しきれない。残り続けると、どれが正しいか分からなくなる。
- 共通の内容と固有の内容が同じ文書に書かれ、共通の改善を各プロジェクトへ手で写している。
- 配布と再現の仕組みまで自作し、保守に時間を取られている。

agent-harness は、すべてを自作しない。  
標準が定まっている形式や、既にある基盤には、それを使う。  
スコープにするのは、足りない部分と、プロジェクトに固有の部分だけである。

## Background

### モデルが更新されるたびに、ハーネスの見直しが要る

OpenAI は GPT-6 Astra の公開に合わせ、スキル、AGENTS.md、タスクのプロンプトを見直すよう開発者に求めている。以前のモデルを導くために足した指示が積み上がっており、細かすぎる手順は今のモデルでは結果を悪くするため。

- 出典: [Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)

リポジトリのスキルは他の貢献者のエージェントも導くため、どのモデルが読むかを考えて書くようにも勧めている。

Anthropic も同じ見方を示している。

- 出典: [Harness design for long-running application development](https://www.anthropic.com/engineering/harness-design-long-running-apps)

ハーネスの各構成要素は「モデルが単独ではできないこと」の仮定を含み、その仮定はモデルの改善で古くなる。  
用途ごとに専用のエージェントを作る設計も、同じ形で負債になると指摘されている。

- 出典: [エージェント開発における悪魔の自転車というアンチパターン](https://x.com/voidwarriorchan/status/2100428460309311607)

プロンプト、権限、引き継ぎが増え、管理の仕組みが本体より大きくなるためである。

### 規約ファイルに書く価値があるのは、モデルが知らない事実である

規約ファイルの効果を測った研究では、規約ファイルは一般にはタスクの成功率を上げず、推論コストを平均 20% 以上増やした。  
具体的な指示はよく守られ、リポジトリの概要説明は役に立たなかった。

- 出典: [Evaluating AGENTS.md: Are Repository-Level Context Files Helpful for Coding Agents?](https://arxiv.org/abs/2602.11988)

Claude Code の公式ガイドも、規約ファイルの内容を、モデルが推測できないコマンドと標準と異なる規約に絞るよう勧める。  
コードから読み取れる内容は除く。  
同じガイドは、規約ファイルの指示は助言にとどまり、フックは必ず実行されると区別している。

- 出典: [Best practices for Claude Code](https://code.claude.com/docs/en/best-practices)

### AI が書いた文書は、読み手に負担を移す

見た目は整っているが、作業を前に進める中身を欠く AI の生成物は workslop と呼ばれる。BetterUp Labs と Stanford Social Media Lab が名づけた。  
米国のフルタイム労働者 1,150 人の調査では、41% が直近 1 か月に workslop を受け取り、1 件の処理に平均 1 時間 56 分かかっていた。

- 出典: [AI-Generated "Workslop" Is Destroying Productivity](https://hbr.org/2025/09/ai-generated-workslop-is-destroying-productivity)

同じ媒体の後続の記事は、workslop が工程の中で連鎖すると、後の工程が確認をやめ、組織の知識の正確さが落ちると指摘している。

- 出典: [Don't Let AI Slop Muck Up Your Company's Processes](https://hbr.org/2026/06/dont-let-ai-slop-muck-up-your-companys-processes)

エージェントに書かせた spec は、この形になりやすい。  
agent-harness が Design Doc、context、ADR を短く保って唯一の参照先とし、spec を作業の後に削除するのは、この問題への対策である。

### Ubiquitous Language

- **コーディングエージェント:** Claude Code、Codex CLI、Cursor など、モデルにコードや文書の作業をさせるツール。本文で単に「エージェント」と書く場合は、その中で作業するモデルを指す
- **ハーネス:** コーディングエージェントに導入する規約・スキル・プラグイン・フック・設定の総称
- **拡張機能:** コーディングエージェントに追加するスキル、プラグイン、フックなどの 1 つ 1 つ。Claude Code の公式文書の呼び方に合わせている
- **規約ファイル:** AGENTS.md や CLAUDE.md のように、セッションの開始時に読み込まれる指示のファイル
- **プロジェクト固有の知識:** モデルが学習やコードの読解だけでは分からない事実。固有のコマンド、標準と異なる規約、業務知識、設計上の決定
- **context:** プロジェクト固有の知識を書いた、リポジトリ内の文書。現在の内容だけを短く書く
- **spec:** issue ごとに作る設計の下書き。議論と決定の経緯を書き、作業が終わったら削除する
- **ガードレール:** 取り返しのつかない操作を、実行の前にプログラムで止める仕組み。行動への柵であり、モデルへの指示には頼らない
- **自動チェック:** 書かれた成果物が規則に沿うかを、機械で確かめる仕組み。成果物への柵であり、報告はするが操作は止めない
- **境界:** sandbox、権限、最小の到達範囲のように、取り返しのつかない場所へ道を作らない設定。コーディングエージェントの機能であり、利用者が先に引く
- **パッケージマネージャー:** 拡張機能の取得、バージョンの固定、配置、更新を行う既存のツール
- **パッケージ:** agent-harness が配る単位。スキル、フック、テンプレート、自動チェックをまとめたもの
- **マニフェスト:** 使う拡張機能と、そのバージョンの指定を書いたファイル。パッケージマネージャーが読む
- **開発プロセスの標準:** 文書の種類と寿命、開発のプロセスの種類、要求ごとの選択（行うプロセス、反映する時点、分解する時点、レビューの範囲）、チェックのように、チームで揃える取り決め
- **ワークフローのハーネス:** 作業をいつ、どの順に進めるかを制御する、具体の拡張機能のまとまり
- **global:** 利用者のホームに置き、すべてのプロジェクトに適用される構成。コーディングエージェントの公式文書では user scope とも呼ぶ
- **サードパーティの拡張機能:** agent-harness と利用者のどちらも作っていないスキルとプラグイン

ハーネス、ガードレール、境界の分け方は、ハーネス設計の入門の整理に合わせている。この文書では、ハーネスを規約からガードレールまでを含む広い意味で使う。

- 出典: [ハーネス設計入門 基礎知識の整理から実務へのステップアップ](https://speakerdeck.com/kinopeee/hanesu-sekkei-nyuumon-kiso-chishiki-no-seiri-kara-jitsumu-heno-suteppu-appu)

ガードレールの語は、分野で意味の幅が違う。LLM の応用の分野では入力と出力の検証まで含む広い意味で使い、コーディングエージェントの分野では実行の前に操作を止めるフックを指すことが多い。この文書は後者の狭い意味で使い、成果物を確かめるものは自動チェックと呼ぶ。

- 出典: [Guardrails - OpenAI Agents SDK](https://openai.github.io/openai-agents-python/guardrails/) — 入力と出力の検証としての用法
- 出典: [Automate actions with hooks](https://code.claude.com/docs/en/hooks-guide) — ツールの呼び出しを拒否するフックを guardrail hook と呼ぶ用法

## Goals

対象ユーザーは、複数のリポジトリとコーディングエージェントを使う個人開発者、プロジェクトの開発者、パッケージの維持者である。

- **固有の事実を読める:** エージェントが、プロジェクト固有の事実を必要なときに読める。常に読み込むのは、目次を読む指示だけである。
- **設計文書が現在の内容だけを持つ:** 設計文書は現在の内容だけを持ち、実装とずれた可能性を一覧できる。spec は作業が終われば消え、レビューは 1 本の HTML で読める。
- **危険な操作が止まる:** 取り返しのつかない操作は、モデルが指示を守るかどうかに関係なく止まる。許す場面は、利用者が宣言する。
- **ルールが自動で守られる:** 機械で判定できるルールは、指示ではなく自動チェックで守られる。
- **共通の内容は 1 か所にある:** どのプロジェクトでも同じ内容はパッケージにあり、プロジェクトには固有の内容だけがある。同じマニフェストから同じ構成を再現でき、基盤以外のパッケージは 1 つずつ外せる。
- **開発プロセスが揃う:** 開発プロセスの種類と要求ごとの選択がチームで揃い、ワークフローのハーネスは選んで差し替えられる。
- **どこでも同じように動く:** Claude Code と Codex CLI で同じように動く。Cursor は可能な範囲で対応し、コーディングエージェントごとの制限は明示する。第三者は README だけで導入でき、CI は対話なしでチェックを実行できる。

## Lower-Priority Goals

- 作業で得た知見を context へ書き戻す作業の支援
- 複数のリポジトリの状態の一括確認。最初のバージョンの対象は、指定した global の環境またはプロジェクト
- Cursor への全面的な対応と、対応するコーディングエージェントの追加
- 公開の marketplace への登録

## Non-Goals

- 拡張機能の取得、バージョンの固定、配置、更新、依存の解決を行う仕組みの自作。パッケージマネージャーを使う。
- 具体のワークフローのハーネスを、このリポジトリに置くこと。別のリポジトリで管理し、利用者が選んで導入する。
- 工程の状態や分岐を管理するワークフローの実行エンジン
- 用途別の専用エージェントと、それらを連携させる仕組み
- コードから読み取れる内容を context として自動生成する機能
- 認証、モデルとの対話、会話の管理、sandbox などコーディングエージェントの機能
- サードパーティの拡張機能の本文の複製と書き換え
- 個人の非公開情報、認証情報、特定の組織の業務知識を含む配布
- 既存の配布の仕組みからの移行の支援。移行は、移行元の仕組みの側で行う。

## User Stories

### 積み上げた指示が足かせになる

- モデルの弱さを補うために足した手順や役割分割が残り、新しいモデルの挙動を縛っている。
- 何のために入れた指示か分からず、外すと何に影響するか判断できない。

**How Might We**

> どうすれば、モデルが進化しても必要な内容と不要になる内容を区別し、不要な内容を安心して外せるか？

### 設計文書から現在の事実を読み取れない

- 設計文書に issue 番号、変更履歴、過去の経緯が入り込み、エージェントも人も余分な記述を読んでいる。
- エージェントが書いた長い spec が残り続け、どの文書が正しいか分からない。
- spec が多くの Markdown に分かれ、レビューする人の負担が大きい。
- 文書が実装とずれても気づけない。更新日を手で書く運用では、更新し忘れを検出できない。

**How Might We**

> どうすれば、参照する文書を現在の内容だけに短く保ち、レビューの負担を抑え、実装とのずれを自動で見つけられるか？

### 守らせたいルールが守られない

- 保護ブランチへの直接コミットの禁止を規約ファイルに書いているが、指示は助言にとどまり、守られない場合がある。
- 一方で、リポジトリの立ち上げの時期のように、直接コミットしてよい場面もある。

**How Might We**

> どうすれば、取り返しのつかない操作を、モデルが指示を守るかどうかに頼らず防ぎ、許す場面は利用者が決められるか？

### 別のマシンや CI で同じ環境を用意できない

- 導入の手順が、個人の設定やローカルの clone に依存している。
- global に入れた拡張機能がプロジェクトの作業手順の暗黙の前提になり、他の開発者や CI では同じ手順が動かない。

**How Might We**

> どうすれば、個人の設定に依存せず、マニフェストから同じ構成を再現できるか？

### 配布の仕組みの保守に時間を取られる

- 共通の規約やスキルを配る仕組みを自作し、コーディングエージェントの仕様変更のたびに直している。
- 配布方式の見直しを繰り返し、知識とガードレールの整備が進まない。

**How Might We**

> どうすれば、既にある標準と基盤を使い、足りない部分と固有の部分に集中できるか？

### 共通の内容をプロジェクトごとに複製する

- テストコマンドや出力先がスキルの本文に書かれ、プロジェクトごとに複製して修正している。
- 文体や文書の品質基準のように、チームで揃えたい規約も、各プロジェクトに写している。
- 共通の改善は、各コピーへの反映が要る。

**How Might We**

> どうすれば、共通の内容を 1 か所で管理し、プロジェクトには固有の内容だけを置けるか？

## Proposed Solution / Feature Description

### 内容の分類と提供範囲

ハーネスの内容を 5 つに分け、種類ごとに扱いを変える。  
パッケージへ何かを足すときは、どれに当たるかを判定する。  
モデルの弱さを補う指示に当たるものは足さない。

| 種類 | 例 | モデルが進化したとき | agent-harness での扱い |
| --- | --- | --- | --- |
| プロジェクト固有の知識 | 固有のコマンド、パス、標準と異なる規約、業務知識、設計上の決定 | 必要であり続ける | テンプレートと書き方のルールを提供。中身は利用者が書く |
| ガードレール | 保護ブランチの保護、秘密情報の混入の防止、権限の設定 | 必要であり続ける。エージェントの権限が広がるほど重要 | 提供 |
| 自動チェック | 文書、リンク、spec の消し忘れのチェック | 必要であり続ける | 提供 |
| 開発プロセスの標準 | 文書の種類と寿命、開発のプロセスの種類、要求ごとの選択、ブランチ名の形式 | 必要であり続ける。チームの合意であり、モデルは知り得ない | 取り決めと、プロセスの中で使う手段を提供。具体の値と順序は利用者が決める |
| モデルの弱さを補う指示 | 段階を踏んで考えさせる指示、役割ごとのエージェントへの分割、考え方の指定 | 不要になる可能性が高い | 提供しない。利用者が選んで別に導入できる |

### 固有の知識の置き場

固有の知識は、利用者のリポジトリの context に置く。機械が読む値は 1 つのファイルに、モデルが読む事実は種類ごとの文書に分ける。

- 書く内容は、モデルがコードを読んでも分からない事実に限る。コードから読み取れる構造の説明は書かない。
- 常に読み込まれるのは、目次を読む指示だけである。エージェントは目次を開き、必要な文書だけを読む。
- 値は、テストコマンド、出力先、ラベル名、保護するブランチのように、リポジトリごとに違うものである。必要な値が足りない場合は、その値を使う処理の前に、足りない値を表示する。
- ツールの具体は扱わない。リポジトリの管理や作業ツリーの管理に何を使うかは、利用者に委ねる。利用者は必要なら context に書き、モデルが解釈する。
- テンプレートは最小の節だけを持ち、経緯を書く欄を設けない。

### 設計文書と spec の寿命

Design Doc と context は、現在の内容だけを短く持つ。ADR は、選択肢を比較して決めた判断を追記していく記録である。  
この 3 つが、食い違ったときの唯一の参照先になる。

どの情報をどこに書くかは、次のとおり。

- 現在の設計、取り決め、制約は、Design Doc と context に書く。
- 選択肢を比較して決めた判断とその理由は、ADR に書く。
- 改訂の日付と順序は、バージョン管理の履歴で追う。
- 作業ごとの議論と決定の経緯は、spec、issue、レビューの記録に残す。

spec は、作業中はその作業の決定を書く場所である。  
残す設計を Design Doc と context へ移した後は、食い違ったら移した先を正しいものとして扱う。作業が終わったら spec は削除する。  
長い下書きを残さないことが、workslop を積み上げないための仕組みになる。

- 削除し忘れた spec は、チェックが報告する。
- 削除は、自動で作られる pull request を人が取り込む形で行う。取り込むときに、残す設計を Design Doc、context、ADR へ移し終えたことを人が確かめる。
- spec が複数の Markdown に分かれていても、レビューする人は 1 つの画面で読める。図も同じ場所で読める。
- 実装とずれた可能性のある Design Doc と context を、一覧できる。一覧は見直しの作業リストであり、チェックの失敗としては扱わない。

### ガードレールと自動チェック

- 最初の対象は、保護ブランチへの直接コミットの防止と、秘密情報がコミットや文書に混入することの防止
- 直接コミットは、止める場面と許す場面を利用者が設定できる。既定は止める。
- 1 つずつ選んで導入できる。
- 働かないコーディングエージェントがある場合は、その制限を対応表に示す。
- 機械で判定できるルールは、スキルや規約ファイルへの指示ではなく、自動チェックで持つ。
  - スキルが入っていない環境や CI でも、同じ結果になる。
  - 文書を編集した直後にも、自動チェックが動く。

### 共通の内容の管理

- どのプロジェクトでも同じ内容は、パッケージとして 1 か所で管理する。
  - 文体、コミット、コードのコメントのように、チームやプロジェクトで揃える規約も、パッケージの一部として選んで導入できる。
  - 同じことを、複数のパッケージで指示しない。
- パッケージの本文には、固有の名前を書かない。利用者によって違うツールの名前も書かず、ツールごとのパッケージも作らない。
- 利用者のリポジトリに共通の基盤を置くパッケージが 1 つあり、ほかのパッケージはそれを前提にする。基盤以外のパッケージは、互いに依存させない。

### 構成の再現

- 拡張機能は、使うプロジェクトのマニフェストに書く。global には入れないことを既定にする。
  - マニフェストを見れば、そのプロジェクトで働く拡張機能がすべて分かる。
  - global だけにある拡張機能を、チームや CI の暗黙の前提にしない。
  - global に置いてよいのは、チャットでの口調のように成果物に影響しない個人の好みと、自分がマニフェストを置けないリポジトリで使う拡張機能に限る。
- 再現、バージョンの固定、更新、差分の確認には、パッケージマネージャーの機能を使う。
- パッケージマネージャーで確認できないことは、不足を実機で確かめたものに限り、環境を変更しないチェックで補う。
- パッケージは、特定のパッケージマネージャーに依存しない形式で公開する。
  - パッケージマネージャーを使わない利用者も、コーディングエージェントの標準の方法で導入できる。

### 開発プロセス

作業をどう進めるかのうち、チームで揃える取り決めを提供する。  
定めるのは、プロセスの種類と、要求ごとの選択だけである。  
どの順に、どの時点で行うかは定めない。

- プロセスは、要件定義、設計、実装、検証の設計、検証の実施、取り込み、リリースの 7 つとする。
  - それぞれに、目的、成果物、完了の条件を定める。
  - 順序は定めない。繰り返すことも、並行して行うことも、前のプロセスへ戻ることも認める。ライフサイクルのモデルを問わない。
  - プロセスを足すかどうかは、利用者が決める。
- レビューは、どのプロセスにも付けられる。
- 要求やタスクごとの選択を定め、選択肢と既定の値を示す。
  - どのプロセスを行うか。
  - Design Doc と ADR へ反映する時点。
  - タスクへの分解と、実装の issue の起票の時点。
  - レビューの要否と範囲。
- 選んだ結果と進み具合は、機械で読める形で記録する。プロジェクトの既定と、要求ごとの選択を分けて持つ。要求ごとの記録は、spec と一緒に削除する。
- タスクの種類ごとの流れは用意しない。種類の違いは、選択の組み合わせで表す。
- 作業を次へ進める制御は、提供しない。人とコーディングエージェントが、記録を読んで進める。具体のワークフローのハーネスを導入した場合は、ハーネスが行う。
- 基準のブランチ、保護するブランチ、バージョンの付け方、リリースの方法のような具体の値は、利用者が決める。
- 開発プロセスの標準に、モデルの弱さを補う指示を混ぜない。段階ごとに止めて確認させる指示が、その例である。

### 見直しと他のハーネスとの併用

- 基盤以外のパッケージは 1 つずつ外せる。基盤への依存のほかに、1 つを外すと他が動かなくなる依存を作らない。
- 新しいモデルが出たら、導入しているサードパーティの拡張機能を見直す。見直しの観点を提供する。
- 具体のワークフローのハーネスを別に導入できる。
  - agent-harness のパッケージは、特定のワークフローのハーネスを前提にしない。
  - どのハーネスを使うか、途中で切り替えるかは、利用者が決める。

## Success Metrics

各項目は、満たす条件と、その確認方法の組である。

### 固有の知識の置き場

- **エージェントがプロジェクト固有の知識を使って作業できる:** 固有のコマンドが要る代表的な作業で、エージェントが context のコマンドを使うことを確認する。
- **常に読み込まれる量が増えない:** context を足しても、常に読み込まれるファイルの内容が変わらないことを確認する。
- **パッケージの本文を変えずに、プロジェクトごとの値を使える:** 2 つのプロジェクトで、同じパッケージに違う値を渡せることを確認する。必要な値の不足も検出する。
- **パッケージが具体のツールに依存しない:** パッケージの本文に、利用者によって違うツールの名前がないことを確認する。

### 設計文書と spec の寿命

- **設計文書が現在の内容だけを持つ:** テンプレートに変更履歴と経緯の欄がないことを確認する。経緯を含む文書をチェックが指摘することも確認する。
- **spec を削除し忘れない:** close 済みの issue の spec が残っているときに、チェックが報告することを確認する。削除の pull request が自動で作られることも確認する。
- **spec を 1 つのまとまった形で読める:** 複数のファイルに分かれた spec から、図を含むレビュー用の 1 つの文書を作れることを確認する。
- **実装とずれた可能性のある文書を一覧できる:** 文書が説明している範囲のファイルを変更すると一覧に出て、範囲の外の変更では出ないことを確認する。

### ガードレール

- **ガードレールが確実に働く:** エージェントに保護ブランチへの直接コミットを指示し、拒否されることを確認する。規約ファイルを空にしても結果が変わらないことも確認する。
- **許す場面を利用者が設定できる:** 直接コミットを許す設定にした場面では通り、それ以外では拒否されることを確認する。
- **自動チェックが、モデルの指示の守り方に左右されない:** 規約ファイルとスキルを外した状態でも、使わない用語を含む文書を自動チェックが指摘することを確認する。

### 共通の内容の管理

- **パッケージが、モデルの弱さを補う指示を含まない:** 各パッケージの Design Doc の「Scope」が、プロジェクト固有の知識、ガードレール、自動チェック、開発プロセスの標準のどれに当たるかを書いていることを確認する。
- **基盤以外のパッケージを 1 つずつ外せる:** 基盤以外のパッケージを 1 つずつ外して、残りが動くことを確認する。サードパーティの拡張機能を外したときに、統合された設定も消えることを確認する。

### 開発プロセス

- **要求ごとに開発プロセスを変えられる:** 小さな変更と大きな変更で、行うプロセスと、Design Doc へ反映する時点を変えられることを確認する。宣言が schema に合わない場合に、チェックが報告することも確認する。
- **ワークフローのハーネスを差し替えられる:** 自作の最小のワークフローのハーネスで、同じ開発プロセスの標準とチェックが動くことを確認する。

### 見直しと他のハーネスとの併用

- **サードパーティのワークフローのハーネスと併用できる:** サードパーティのハーネスを 1 つ導入し、context、ガードレール、チェックが変わらず動くことを確認する。

### 構成の再現

- **構成を再現できる:** 何も入っていない環境で同じマニフェストから導入し、バージョンの一致を確認する。global に拡張機能がない環境でも、プロジェクトの構成だけで導入できることを確認する。
- **差分の確認が環境を変えない:** 確認の前後で、拡張機能、設定、マニフェストが変わらないことを確認する。
- **Claude Code と Codex CLI で動く:** 2 つのコーディングエージェントとパッケージの組み合わせごとに、導入から代表的な利用までを確認する。Cursor は、確認できた範囲を対応表に示す。

### 公開

- **第三者と CI が手順を再現できる:** 前提を満たす環境で、第三者が README だけで導入できることを確認する。CI がチェックを対話なしで実行できることも確認する。

## Milestones

到達する状態で定める。順に到達する。

1. **自分で運用している:** このリポジトリ自身が、基盤、文書の体系、ガードレールのパッケージで運用されている。
2. **第三者が導入できる:** 第三者が README だけで導入でき、CI がチェックを対話なしで実行し、最初のタグが付いている。
3. **併用を確かめてある:** 開発プロセスのパッケージが動き、サードパーティのワークフローのハーネスとの併用が確かめてある。

## Open Questions / Risks

### リスク

- **採用したパッケージマネージャーの開発が止まる、または仕様が大きく変わる。**  
  再現と更新の手段を失う。備えとして、パッケージを特定のパッケージマネージャーに依存しない形式で公開し、マニフェストとロックファイルのほかに、パッケージマネージャー専用のものを持たない。
- **context の効果が出ない。**  
  規約ファイルが成功率を上げなかった研究結果があり、保守の手間だけが増えるおそれがある。書く内容をモデルだけでは分からない事実に限り、効果と読み込まれる量を Success Metrics で確認する。
- **ワークフローのハーネスを入れていない利用者で、spec から Design Doc や context への移し忘れが増える。**  
  agent-harness 自身は、作業を次へ進める制御を持たないためである。spec の削除で、残すべき設計を失う。削除を自動では行わず、人が自動で作られた pull request を取り込むときに確かめる形で防ぐ。
- **コーディングエージェントの仕様変更で、ガードレールが働かなくなる。**  
  止めたい操作が通る。対応表に確認したコーディングエージェントのバージョンを記録し、ガードレールの確認をいつでも再実行できる形で残す。

## References

- [Rethinking skills and prompts for GPT-6 Astra](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra) — 新しいモデルに合わせたスキルと AGENTS.md の見直し
- [Harness design for long-running application development](https://www.anthropic.com/engineering/harness-design-long-running-apps) — ハーネスの構成要素が含む仮定と、その陳腐化
- [エージェント開発における悪魔の自転車というアンチパターン](https://x.com/voidwarriorchan/status/2100428460309311607) — 用途別の専用エージェントを増やす設計の問題
- [Evaluating AGENTS.md: Are Repository-Level Context Files Helpful for Coding Agents?](https://arxiv.org/abs/2602.11988) — 規約ファイルの効果の評価
- [Best practices for Claude Code](https://code.claude.com/docs/en/best-practices) — 規約ファイルに書く内容と、フックとの違い
- [AI-Generated "Workslop" Is Destroying Productivity](https://hbr.org/2025/09/ai-generated-workslop-is-destroying-productivity) — workslop の定義と調査の結果
- [Don't Let AI Slop Muck Up Your Company's Processes](https://hbr.org/2026/06/dont-let-ai-slop-muck-up-your-companys-processes) — workslop の連鎖による組織の知識の劣化
