commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}

pre-commit:
  commands:
    index:
      run: node packages/core/hooks/build-index.mjs
    textlint:
      glob: "*.md"
      run: pnpm exec textlint {staged_files}
