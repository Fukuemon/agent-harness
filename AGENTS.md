# agent-harness

- 作業を始める前に `context/index.md` を読み、作業に関係する文書を開く。
- このリポジトリの文書は常体で書く。global の文体の指定より優先する。
- `.claude/skills/` と `.agents/skills/` を直接編集しない。`apm install` が作る写しで、次の実行で上書きされる。元は `skills/` にある。
- 公開しない作業メモは `.ai-out/` に置く。PRD、Design Doc、ADR には書かない。
