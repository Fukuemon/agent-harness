# agent-harness

- このリポジトリの文書は常体で書く。global の文体の指定より優先する。
- スキルの元は `skills/` にある。`.claude/skills/` と `.agents/skills/` は、`apm install` が作る写しである。
  - 写しを直接は直さない。次の `apm install` で上書きされる。
  - `skills/` を直したら、`apm install` を実行して写しへ反映する。
- 公開しない作業メモは `.ai-out/` に置く。移植元のリポジトリの名前が当たる。PRD、Design Doc、ADR には書かない。
